{% extends "base.html.twig" %}


{% block body %}


	{{form_start(userForm)}}

	{{form_label(userForm.email,'Email :', {'label_attr' : {'class' : 'label'}})}}
	{{form_widget(userForm.email, {'attr' : {'class' : 'input', 'placeholder' : 'Email'}})}}

	{{form_label(userForm.password,'Mot de passe :', {'label_attr' : {'class' : 'label'}})}}
	{{form_widget(userForm.password, {'attr' : {'class' : 'input', 'placeholder' : 'Mot de passe'}})}}

	{{form_widget(userForm.Enregistrer, {'attr' : {'class' : 'input',}})}}


	{{form_end(userForm)}}
{% endblock %}
