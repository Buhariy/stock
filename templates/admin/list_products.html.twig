{% extends 'base.html.twig' %}

{% block title %}Hello AdminProductController!
{% endblock %}

{% block body %}
	<div class="container">

		<table class="table">
			<thead>
				<tr>
					<th scope="col">ID</th>
					<th scope="col">Name</th>
					<th scope="col">Action</th>
				</tr>
			</thead>
			<tbody>
				{% for product in products %}
					<tr>

						<td>{{product.id}}</td>
						{# <td>
																			<a href="{{path('front_show_product', {id : product.id})}}">{{product.name}}</a>
																		</td> #}
						<td>{{product.id}}</td>
						<td>{{product.name}}</td>
						<td><img src="{{asset("media/" ~ product.media[0].src)}}" alt="{{product.media[0].alt}}" width="100px"></td>
						{# <td>
																																					<a href="{{path('admin_update_product', {id : product.id})}}">
																																						<i class="fa-solid fa-pen-to-square"></i>
																																					</a>
																																					<a href="{{path('admin_delete_product', {id : product.id})}}">
																																						<i class="fa-solid fa-trash-can"></i>
																																					</a>
																																				</td> #}


					</tr>
				{% endfor %}
			</tbody>
		</table>
		{# <div class="container">
																															<a href="{{path('admin_create_product')}}" class="btn btn-success">Créer un produit</a>
																														</div> #}
	</div>
{% endblock %}
